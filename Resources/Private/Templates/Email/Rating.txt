<html data-namespace-typo3-fluid="true" lang="en" xmlns:f="http://typo3.org/ns/TYPO3/CMS/Fluid/ViewHelpers">
<f:layout name="SystemEmail" />
<f:section name="Title">{headline}</f:section>
<f:section name="Main">
    <f:if condition="{rating}">
        Rate Value: {rating.rateValue}
        Partner: {rating.partner.title} ({rating.partner.partnerNr})
        <f:if condition="{rating.reason}">Reasons:<f:for each="{rating.reason}" as="reason">
            Reason: {reason.title}
            Description: {reason.description -> f:format.stripTags()}</f:for></f:if>
        <f:if condition="{rating.reasonText}">Reason Text: {rating.reasonText}</f:if>
    </f:if>
</f:section>
</html>
