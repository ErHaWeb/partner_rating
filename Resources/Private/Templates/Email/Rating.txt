<html data-namespace-typo3-fluid="true" lang="en" xmlns:f="http://typo3.org/ns/TYPO3/CMS/Fluid/ViewHelpers">
<f:layout name="SystemEmail" />
<f:section name="Title">{headline}</f:section>
<f:section name="Main"><f:if condition="{rating}">
<f:translate key="mail.rating.department" extensionName="PartnerRating" />: {rating.department.title}
<f:translate key="mail.rating.date" extensionName="PartnerRating" />: <f:format.date format="{dateFormat}">{rating.ratingDate}</f:format.date>
<f:translate key="mail.rating.rateValue" extensionName="PartnerRating" />: {rating.rateValue}
<f:translate key="mail.rating.partner" extensionName="PartnerRating" />: {rating.partner.title} ({rating.partner.partnerNr})<f:if condition="{rating.reason}">
<f:translate key="mail.rating.reasons" extensionName="PartnerRating" />:
<f:for each="{rating.reason}" as="reason">- <f:translate key="mail.rating.reason" extensionName="PartnerRating" />: {reason.title}
  <f:translate key="mail.rating.description" extensionName="PartnerRating" />: {reason.description -> f:format.stripTags()}
</f:for></f:if><f:if condition="{rating.reasonText}">
<f:translate key="mail.rating.reasonText" extensionName="PartnerRating" />: {rating.reasonText}</f:if><f:if condition="{user.uid} > 0">
<f:translate key="mail.rating.user" extensionName="PartnerRating" />: <f:if condition="{user.first_name} && {user.last_name}"><f:then>{user.first_name} {user.last_name}</f:then><f:else>{user.username}</f:else></f:if></f:if></f:if></f:section>
</html>
